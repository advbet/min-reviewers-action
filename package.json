{
  "name": "min-reviewers-action",
  "main": "index.js",
  "scripts": {
    "build": "ts-standard --fix index.ts && ncc build build/index.js -o dist --source-map --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.0",
    "ts-standard": "^12.0.2",
    "typescript": "^4.9.4"
  }
}
