{
  "name": "min-reviewers-action",
  "main": "index.js",
  "scripts": {
    "build": "standard --fix index.js && ncc build index.js -o dist --source-map --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.0",
    "standard": "^17.0.0"
  }
}
